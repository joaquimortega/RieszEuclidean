# Averaging and conditional geometric conclusions

This records the intermediate averaging/geometry checkpoint before the Bochner
construction and unconditional wrappers closed the remaining mathematical nodes.
Thirty-one new modules add 280 public declarations. The modular project builds
with warnings as errors; all 15 modular linters pass on 960 declarations and
612 generated declarations. The thirty-result public reference also builds and
passes all 15 linters on 33 declarations and 5 generated declarations.

Canonical scalar test vectors have the exact averaged energy. Actual Fourier
and bump projection pairings, scalar Cauchy–Schwarz and strong operator limits
transfer the uniform hull gap to the stationary cutoffs without an auxiliary
amplified Hilbert space. Ball, noncollinear triangle, affine ellipsoid, general
boundary and maximal-side polygon contradictions are assembled from actual
exponential Riesz bases. Their only remaining analytic assumption is explicit
existence of representing spectral measures on stationary hulls. The subsequent
Bochner modules construct those measures and discharge the hypotheses in
unconditional wrappers.

Polygon supporting faces are identified with literal maximal boundary segments,
with the same side count and geometric parallelism. Positive normalization
removes unit-normal assumptions. The interval/rectangle overlap examples and
the ordinary translation representation's explicit spectral density are also
implemented; the latter has no nonzero invariant vector in positive dimension.

The source label rem:stationary-space was added to the existing explanatory
remark, and rem:interval is now tracked by the blueprint. No mathematical
manuscript text was changed by these label and inventory updates.

The 877-declaration transitive axiom audit for this intermediate batch uses only
standard Lean axioms. The later Bochner and ball/ellipsoid batch adds 50 audited
declarations, also using only standard Lean axioms. The later 37-target
unconditional reference and final audit are recorded in `bochner-progress.md`.
The accepted twenty-three-result checkpoint 0fb31ee and its exact inputs remain
recorded separately in comparison-sphere-progress.md and its hash manifests.
